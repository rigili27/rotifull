{
    "build": {
      "dockerfilePath": "Dockerfile"
    },
    "deploy": {
      "startCommand": "php artisan serve --host=0.0.0.0 --port=8000",
      "postdeployCommand": "php artisan migrate --force && php artisan db:seed --force"
    }
  }
  